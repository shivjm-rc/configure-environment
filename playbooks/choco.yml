---
- name: Install Chocolatey apps
  hosts: windows
  vars:
    apps: "{{ lookup('file', 'chocolatey-packages').strip().split() }}"
  tasks:
    - win_chocolatey:
        name: "{{ item }}"
        state: latest
      loop: "{{ apps | flatten(level=1) }}"
